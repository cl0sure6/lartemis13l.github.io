# Hugo build output and caches (generated — never commit)
/public/
/resources/_gen/
.hugo_build.lock

# Hugo lock/temp
hugo_stats.json

# OS / editor cruft
.DS_Store
Thumbs.db
*~

# Local ops script — deliberately not published in the public repo
cloudflare-rules.sh
